# Managed by infra/remote-setup.sh — defense in depth over the cloud-init
# drop-ins (which already disable password auth). Root logs in by key only.
PermitRootLogin prohibit-password
PasswordAuthentication no
KbdInteractiveAuthentication no
X11Forwarding no
